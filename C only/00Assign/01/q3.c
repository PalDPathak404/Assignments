#include <stdio.h>

int main()
{
  printf("This is the sentence where the next words will be printed in the next line \n as an operator is used here also now will come two spaces as I used an operator \t for it which consists of a \\ which is \"compulsory\" for \'using\' them like I used another two double and single quote operators");
}
